# Environment variables for the SlotSwapper Backend
# For local development, rename this file to .env and fill in the values.

# -- Database Configuration --
# For Docker setup, DB_HOST must be the service name from docker-compose.yml (e.g., 'db')
# For local setup without Docker, it would be 'localhost'
DB_HOST=db
DB_USER=postgres
DB_PASSWORD=your_secret_password_here
DB_DATABASE=slotswapper
DB_PORT=5432

# -- Authentication Configuration --
# Use a long, random, and secret string for JWT signing
JWT_SECRET=your_super_long_random_jwt_secret_here